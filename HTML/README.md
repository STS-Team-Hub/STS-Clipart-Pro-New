# HTML reference fixtures

This folder stores raw copied HTML samples used as reference material while developing and validating site profiles.

Keep this directory flat: place samples directly under `HTML/` and do not recreate nested folders such as `HTML/HTML/`.
